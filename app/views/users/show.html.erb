<p id="notice"><%= notice %></p>


<div class="jumbotron jumbotron-form text-align-left">
  <div class="row">
    <div class="col-sm-3">
      <strong>Name:</strong>
      <%= @user.name %>
    </div>

    <div class="col-sm-3">
      <strong>Department:</strong>
      <%= @user.department %>
    </div>
  </div>
</div>

<%= link_to 'Edit', edit_user_path(@user), class: "btn btn-sm btn-primary text-dark"%>
<%= link_to 'Back', users_path, class: "btn btn-sm btn-primary text-dark"%>
